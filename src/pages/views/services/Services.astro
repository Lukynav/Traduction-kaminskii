---
import Title from "@components/title/Title.astro";
import Card from "./card/Card.astro"
import { SERVICES } from "@models/consts/index"
---
<Title title="Servicios de traducción" />
<section class="max-w-3xl m-auto mt-6 grid grid-cols-1 p-4 md:p-2 md:grid-cols-2 gap-4">
    {SERVICES.map(service => {
        return <Card {...service} />
    })}
</section>
